    spring.security.user.name=MMaster
    spring.security.user.password=UFPAP2
    spring.security.user.roles=USER


    # Faz o Hibernate apagar e criar as tabelas do zero ao iniciar
    spring.jpa.hibernate.ddl-auto=update

    # Mostra o SQL no console para a gente ter certeza que ele limpou
    spring.jpa.show-sql=true
    spring.jpa.properties.hibernate.format_sql=true

    # Garante que ele use o dialeto correto do PostgreSQL
    spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect